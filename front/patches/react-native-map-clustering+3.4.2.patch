diff --git a/node_modules/react-native-map-clustering/lib/ClusteredMapView.js b/node_modules/react-native-map-clustering/lib/ClusteredMapView.js
index 9e44320..6fe23ff 100644
--- a/node_modules/react-native-map-clustering/lib/ClusteredMapView.js
+++ b/node_modules/react-native-map-clustering/lib/ClusteredMapView.js
@@ -133,9 +133,6 @@ const ClusteredMapView = forwardRef(
         const bBox = calculateBBox(region);
         const zoom = returnMapZoom(region, bBox, minZoom);
         const markers = superCluster.getClusters(bBox, zoom);
-        if (animationEnabled && Platform.OS === "ios") {
-          LayoutAnimation.configureNext(layoutAnimationConf);
-        }
         if (zoom >= 18 && markers.length > 0 && clusterChildren) {
           if (spiralEnabled) updateSpiderfier(true);
         } else {
diff --git a/node_modules/react-native-map-clustering/lib/ClusteredMarker.js b/node_modules/react-native-map-clustering/lib/ClusteredMarker.js
index 73700ba..6fc39b9 100755
--- a/node_modules/react-native-map-clustering/lib/ClusteredMarker.js
+++ b/node_modules/react-native-map-clustering/lib/ClusteredMarker.js
@@ -26,21 +26,9 @@ const ClusteredMarker = ({
       onPress={onPress}
       tracksViewChanges={tracksViewChanges}
     >
-      <TouchableOpacity
-        activeOpacity={0.5}
+      <View
         style={[styles.container, { width, height }]}
       >
-        <View
-          style={[
-            styles.wrapper,
-            {
-              backgroundColor: clusterColor,
-              width,
-              height,
-              borderRadius: width / 2,
-            },
-          ]}
-        />
         <View
           style={[
             styles.cluster,
@@ -65,7 +53,7 @@ const ClusteredMarker = ({
             {points}
           </Text>
         </View>
-      </TouchableOpacity>
+      </View>
     </Marker>
   );
 };
diff --git a/node_modules/react-native-map-clustering/lib/helpers.js b/node_modules/react-native-map-clustering/lib/helpers.js
index 8961171..5f3aafa 100644
--- a/node_modules/react-native-map-clustering/lib/helpers.js
+++ b/node_modules/react-native-map-clustering/lib/helpers.js
@@ -86,63 +86,63 @@ export const generateSpiral = (marker, clusterChildren, markers, index) => {
 export const returnMarkerStyle = (points) => {
   if (points >= 50) {
     return {
-      width: 84,
-      height: 84,
-      size: 64,
+      width: 54,
+      height: 54,
+      size: 54,
       fontSize: 20,
     };
   }
 
   if (points >= 25) {
     return {
-      width: 78,
-      height: 78,
-      size: 58,
+      width: 50,
+      height: 50,
+      size: 50,
       fontSize: 19,
     };
   }
 
   if (points >= 15) {
     return {
-      width: 72,
-      height: 72,
-      size: 54,
+      width: 46,
+      height: 46,
+      size: 46,
       fontSize: 18,
     };
   }
 
   if (points >= 10) {
     return {
-      width: 66,
-      height: 66,
-      size: 50,
+      width: 43,
+      height: 43,
+      size: 43,
       fontSize: 17,
     };
   }
 
   if (points >= 8) {
     return {
-      width: 60,
-      height: 60,
-      size: 46,
+      width: 38,
+      height: 38,
+      size: 38,
       fontSize: 17,
     };
   }
 
   if (points >= 4) {
     return {
-      width: 54,
-      height: 54,
-      size: 40,
+      width: 33,
+      height: 33,
+      size: 33,
       fontSize: 16,
     };
   }
 
   return {
-    width: 48,
-    height: 48,
-    size: 36,
-    fontSize: 15,
+    width: 28,
+    height: 28,
+    size: 28,
+    fontSize: 14,
   };
 };
 
